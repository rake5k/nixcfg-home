---
builds:
  exclude:
    - "homeConfigurations.christian@non-nixos"
    - "homeConfigurations.demo@non-nixos"
    - "nixosConfigurations.nixos"
  include:
    - "checks.*.*"
    - "devShell.x86_64-linux"
    - "homeConfigurations.*"
    - "nixosConfigurations.*"
